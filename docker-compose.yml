services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_DB: gym_schedule
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "55432:5432"
    volumes:
      - gym_postgres_data:/var/lib/postgresql/data

volumes:
  gym_postgres_data:
